# Audit Service

## 启动方式

### Docker 启动（推荐）

使用根目录脚本：

```bash
./build-start-docker.sh
```

### 本地启动

先安装 `opencode`：

```bash
curl -fsSL https://opencode.ai/install | bash
```

再使用根目录脚本启动：

```bash
./start.sh
```
